import React from 'react';
import DeleteIcons from '@material-ui/icons/Delete';

const Delete = () => {
  

  return (
    <button>
      <DeleteIcons />
    </button>
  );
};

export default Delete;
